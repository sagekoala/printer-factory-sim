# Provider app settings
# Leave PROVIDER_DATABASE_URL unset to use the default (provider/provider.db).
# Override only if you want the SQLite file in a non-default location.
# PROVIDER_DATABASE_URL=sqlite:///./provider/provider.db
PROVIDER_PORT=8001
